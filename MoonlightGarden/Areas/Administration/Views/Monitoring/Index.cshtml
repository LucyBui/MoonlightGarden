@model IEnumerable<System.String>

@{
    ViewBag.Title = "Index";
}
<table class="table">
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item)
        </td>
    </tr>
}

</table>
